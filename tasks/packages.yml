- name: Install packages
  apt:
    name: "{{ packages }}"
    state: present
  vars:
    packages:
      - curl
      - expect
      - git
      - htop
      - mc
      - tmux
      - vim
      - zsh
